/*
import React from 'react';
import AddArticle from '../pages/AddArticle';

const LoggedIn = ({ userRole }) => {
    const canEdit = userRole === 'editor';

    return (
        <div>
            {canEdit && <button onClick={AddArticle}>Add Article</button>}
        </div>
    );
};

export default LoggedIn;
*/